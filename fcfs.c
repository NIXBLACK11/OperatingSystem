#include<stdio.h>

int main()
{
    int n = 5;
    int at[n], bt[n], rt[n], tat[n], wt[n], ct[n];
    printf("\nEnter the arrival time:");
    for(int i=0;i<n;i++)
    {
        scanf("%d", at[i]);
    }
    printf("\nEnter the burst time:");
    for(int i=0;i<n;i++)
    {
        scanf("%d", bt[i]);
    }
    
}